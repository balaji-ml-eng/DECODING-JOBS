# Local development environment variables for core-api.
# This file holds non-production defaults only — real secrets belong in a
# secrets manager (AWS Secrets Manager / SSM) from Phase 3 onward.

PROJECT_NAME="DECODING JOBS Core API"
ENVIRONMENT=development
API_V1_PREFIX=/api/v1

DATABASE_URL=postgresql+asyncpg://decoding_admin:decoding_pass_dev@postgis:5432/decoding_jobs

CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000
